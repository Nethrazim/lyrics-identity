apiVersion: v1
kind: Service
metadata:
  name: lyricsidentity
  labels:
    app: lyricsidentity
spec:
  type: NodePort
  selector:
    app: lyricsidentity
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30080
